{
  "name": "pandify",
  "version": "1.0.0",
  "description": "Updates users playlists with songs.",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "dependencies": {},
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^10.2.0"
  },
  "scripts": {
    "test": "mocha --recursive",
    "test-regex": "mocha --recursive -g --",
    "debug-test": "mocha --recursive",
    "debug-test:regex": "npm run debug-test -- -g"
  },
  "author": "",
  "license": "ISC"
}
